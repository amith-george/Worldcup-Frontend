services:
  worldcup-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: worldcup-frontend
    restart: unless-stopped
    ports:
      - "8081:8081"
    networks:
      - worldcup-net

networks:
  worldcup-net:
    external: true
